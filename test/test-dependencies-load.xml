<project xmlns:c="antlib:it.haefelinger.flaka">
  <macrodef name="dependency">
    <attribute name="alias" default=""/>
    <element name="groupId" />
    <element name="artifactId" />
    <element name="version" />
    <sequential />
  </macrodef>

  <c:dependency>
    <groupId>log4j</groupId>
    <artifactId>log4j</artifactId>
    <version>1.2.8</version>
  </c:dependency>

  <dependency alias="log4j">

  </dependency>
  
  <c:inline-properties>
    depot.csv = file://localhost/${basedir}/lib
    build.dir = build
    project.dep.xml = ${ant.file}
    baseline.via = HTTP
    baseline.httproot = http://haefelingerit.net/baseline
    baseline = HEAD
    baseline.rev = HEAD
    baseline.dir = ${build.dir}/baseline
    lib.dir      = ${build.dir}/lib
    depot.csv    = http://haefelingerit.net/depot
  </c:inline-properties>

  <c:dependencies-load debug="true"/>


</project>