<project xmlns:c="antlib:it.haefelinger.flaka" name="xx">
  <c:echo>
  #{ project.name }
  #{ file(project) }
  #{ file('.').absolutePath }
  #{ file(project.basedir).absolutePath }
  ${basedir}
  
  </c:echo>
  <c:echo>
  ;; report last modification time (as Date object)
  Modified at #{ format('%1$tA, %1$tD', file(project).mtime) }
</c:echo>
</project>